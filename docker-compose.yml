services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: hermes_sql
    environment:
      SA_PASSWORD: "SqlServer123!"
      ACCEPT_EULA: "Y"
    ports:
      - "14333:1433"
    volumes:
      - sql_data:/var/opt/mssql

volumes:
  sql_data: